# План оптимизации по результатам аудита

## Цель

Повысить стабильность, поддерживаемость и производительность PCM Helper без изменения пользовательского поведения первой итерацией.

## Контекст

Аудит выполнен 2026-07-01. Проект представляет собой Vue 3/Vite приложение в директории `app` с двумя доменными зонами: PCM-справочник и networker-граф на D3. Производственная сборка `npm run build` успешно проходит при запуске вне sandbox.

## Наблюдения

- В `package.json` скрипт `test:unit` ссылается на `vue-cli-service`, но проект использует Vite и не содержит Vue CLI в зависимостях.
- В `src/router.js` и части стартовых данных видны поврежденные русские строки, похожие на mojibake.
- CSS-бандл сборки около 764 KB до gzip из-за полного подключения Bulma и Font Awesome.
- TypeScript включен в строгом режиме, но проект смешивает `.js`, `.ts` и импорты с явным расширением `.ts`.
- В `localStore.ts` и импорте файлов есть прямой `JSON.parse` без обработки ошибок.
- В D3-слое используется `@ts-ignore`, много `any`, а часть callback-типов не соответствует фактическим данным.
- В `graphStore.ts` демо-данные содержат повторяющийся `id` для functional circles.
- В `DrawNetwork.drawTick` обращение к `this.scope[d.id].circle` может упасть, если круг связан с отсутствующим узлом.
- Сервис PCM возвращает статические JSON-данные через ручные Promise-обертки, хотя операции синхронные.
- Нет зафиксированных правил по форматированию, линтингу, тестам и структуре планов.

## Группа A: Стабильность и корректность

- [ ] A1. Исправить поврежденную кодировку русских строк в `src/router.js` и проверить заголовки страниц в браузере.
- [x] A2. Исправить поврежденную кодировку русских строк в стартовых graph-данных `src/networker/composable/graphStore.ts`.
- [x] A3. Заменить `test:unit` в `app/package.json` на рабочий Vite-compatible test runner или удалить скрипт до внедрения тестов.
- [x] A4. Добавить безопасный JSON-парсер для `localStorage` и применить его в `src/core/composable/store/localStore.ts`.
- [x] A5. Добавить безопасную обработку некорректного JSON при импорте networker-файлов.
- [x] A6. Исправить повторяющиеся `id` в стартовых данных `funcCircles`.
- [x] A7. Добавить защиту в `DrawNetwork.drawTick` от functional circle без связанного node в `scope`.

## Группа B: Типизация и поддерживаемость

- [x] B1. Перевести `src/router.js` на TypeScript и сохранить текущую конфигурацию маршрутов.
- [x] B2. Перевести `src/main.js` на TypeScript и проверить запуск приложения.
- [x] B3. Перевести `src/pcm/service.js` на TypeScript без изменения публичного API.
- [x] B4. Убрать явные `.ts` расширения из импортов там, где это не требуется Vite-конфигурацией.
- [x] B5. Описать типы DTO для networker-импорта и экспорта.
- [x] B6. Описать типы коллекций node, link, circle и tag в graph-сервисах.
- [ ] B7. Уменьшить использование `any` в `GraphService`, не меняя поведение методов.
- [ ] B8. Уменьшить использование `any` в D3-render слое и убрать `@ts-ignore` для импорта D3.
- [x] B9. Упростить PCM-сервис: заменить ручные Promise-обертки на синхронные методы или явно зафиксировать async-контракт.

## Группа C: Производительность и размер сборки

- [x] C1. Измерить текущий размер CSS и JS после `npm run build` и записать значения в план или отдельный отчет. После обновления Vite до `8.1.2`: CSS `756.63 kB` (`88.71 kB gzip`), JS `225.81 kB` (`77.61 kB gzip`).
- [ ] C2. Проверить используемые классы Bulma и подготовить список модулей для выборочного импорта.
- [ ] C3. Перейти с полного импорта Bulma на выборочный импорт Sass/CSS.
- [ ] C4. Найти используемые иконки Font Awesome и подготовить список для точечного импорта.
- [ ] C5. Заменить полный импорт Font Awesome на точечные иконки или локальный icon-компонент.
- [ ] C6. Перевести страницы router на lazy imports.
- [ ] C7. Проверить, что D3/networker-код не попадает в стартовый chunk без открытия networker-страниц.
- [ ] C8. Добавить npm-команду для анализа размера bundle.

## Группа D: UX и хранение данных

- [ ] D1. Добавить версию схемы в экспортируемые networker-файлы.
- [ ] D2. Добавить валидацию импортируемого networker-файла до создания сущностей.
- [ ] D3. Разделить демо-данные graph-store и пользовательское состояние.
- [ ] D4. Добавить обработку ошибок при сохранении в `localStorage`, включая quota exceeded.
- [ ] D5. Проверить поведение приложения при пустом графе и зафиксировать найденные проблемы.
- [ ] D6. Проверить поведение приложения при битых данных в `localStorage` и зафиксировать найденные проблемы.
- [ ] D7. Проверить поведение приложения при неизвестных route params и зафиксировать найденные проблемы.

## Критерии готовности первой итерации

- [x] E1. `npm run build` проходит локально.
- [x] E2. Скрипт тестов не указывает на отсутствующий инструмент.
- [ ] E3. Поврежденные русские строки исправлены.
- [x] E4. Некорректный JSON в `localStorage` или импортируемом файле не ломает приложение.
- [x] E5. В стартовых graph-данных нет повторяющихся идентификаторов.
- [ ] E6. Основные найденные риски оформлены в follow-up задачах или закрыты кодом.
